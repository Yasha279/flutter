import 'package:flutter/material.dart';

class FavoriteUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Favorite Users')),
      body: Center(child: Text('Favorite Users Page')),
    );
  }
}